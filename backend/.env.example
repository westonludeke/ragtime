# Backend environment variables

# Required
OPENAI_API_KEY=sk-your-openai-key

# Chroma configuration (persistent via local Chroma server)
# URL of the Chroma server. Default is http://localhost:8000
# When running Chroma locally with persistence, see README for a Docker command.
CHROMA_URL=http://localhost:8000

# Deterministic collection name so the same collection is opened across restarts
CHROMA_COLLECTION_NAME=ragtime

# Number of embedding dimensions used by your embeddings model
# text-embedding-3-small => 1536, text-embedding-3-large => 3072
CHROMA_NUM_DIMENSIONS=1536

# Optional Express port override
# PORT=3000
